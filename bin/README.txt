# Media encoder cho Linux hosting (x86_64)
#
# Chuẩn bị trên máy local (Windows):
#   php artisan social:media-encoder-install --for-linux --force
#
# Sau đó upload file ffmpeg trong thư mục này lên hosting.
# Không chạy composer install script trên hosting để tải binary.
